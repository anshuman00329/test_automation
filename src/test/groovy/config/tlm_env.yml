app_config:
  shipment:
    url: http://scshipment.${envTag}.manhdev.com
    endpoint: /api/scshipment/shipment
    create_endpoint: /api/scshipment/shipment/save
    get_endpoint: /api/scshipment/shipment/shipmentId/

  order:
    url: http://dcorder.${envTag}.manhdev.com
    endpoint: /api/dcorder/order
    create_endpoint: /api/dcorder/order/import
    get_endpoint: /api/dcorder/order/orderId

  tender:
    url: http://tender.${envTag}.manhdev.com
    endpoint: /api/tender/tender

  tracking:
    url: http://tracking.${envTag}.manhdev.com
    endpoint: /api/tracking/tracking

  accept:
    url: http://tender.${envTag}.manhdev.com
    endpoint: /api/tender/acceptTender

  invoice:
    url: http://invoice.${envTag}.manhdev.com
    endpoint: /api/invoice/invoice

  mq:
    url: tcp://activemq.sc2020devint.manhdev.com:61616
    url456: http://activemq.tlmqe.manhdev.com/admin/xml/queues.jsp


tlmdevint:
  db_config:
    default: &db_default
      host: 10.50.72.139
      port: 10195
      adapter: mysql
      username: 'manhmysql'
      password: 'manhmysql'
      driver: 'com.mysql.cj.jdbc.Driver'
      properties:
        useSSL: false
        requireSSL: false

    shipment:
      <<: *db_default
      service: tlmb_scshipment

    tender:
      <<: *db_default
      service: tlmb_tender

    tracking:
      <<: *db_default
      service: tlmb_tracking

    order:
      <<: *db_default
      service: tlmb_dcorder

    accept:
      <<: *db_default
      service: tlmb_tender

    invoice:
      <<: *db_default
      service: tlmb_invoice

tlmautomationqe:
  db_config:
    default: &db_default1
      host: 10.50.75.107
      port: 10795
      adapter: mysql
      username: 'sc2020'
      password: ''
      driver: 'com.mysql.cj.jdbc.Driver'
      properties:
        useSSL: false
        requireSSL: false

    shipment:
      <<: *db_default1
      service: scshipment

    tender:
      <<: *db_default1
      service: tender

    tracking:
      <<: *db_default1
      service: tracking

    order:
      <<: *db_default1
      service: dcorder

    accept:
      <<: *db_default1
      service: tender

    invoice:
      <<: *db_default1
      service: invoice